# Cloudflare Pages — Vue Router SPA fallback
# 단축 URL 리다이렉트
/v/:id  /s/:id  301

# 단일 브랜드 페이지는 SPA로 처리(legacy html 우선순위 방지)
/brand/*  /index.html  200

# SPA: 모든 경로를 index.html로 (Vue Router가 처리)
/*  /index.html  200
